## 1. Core Implementation

- [x] 1.1 Implement `src/lib/qsToMongo.js` — main function with type coercion, operators, error handling

## 2. Route Integration

- [x] 2.1 Add `qsToMongo` to customer index handler (`GET /api/customers`)
- [x] 2.2 Add `qsToMongo` to place index handler (`GET /api/customers/:customerId/places`)
- [x] 2.3 Add `qsToMongo` to service index handler (`GET /api/customers/:customerId/places/:placeId/services`)

## 3. Verification

- [x] 3.1 Run syntax check on all files
