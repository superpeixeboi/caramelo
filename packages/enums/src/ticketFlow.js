export const TICKET_FLOW = Object.freeze({
  SERVICE_REQUEST: Object.freeze({ value: 'service-request' }),
})

export const TICKET_FLOW_VALUES = Object.freeze(
  Object.values(TICKET_FLOW).map((e) => e.value)
)
